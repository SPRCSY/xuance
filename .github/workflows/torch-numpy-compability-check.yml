name: Torch NumPy Compatibility

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "xuance/**"
      - "tests/**"
      - "setup.py"
      - ".github/workflows/torch-numpy-compatibility.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    strategy:
      fail-fast: false
      max-parallel: 2

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup uv
        uses: astral-sh/setup-uv@v10.1.0
        with:
          enable-cache: true

      - name: Install Python
        run: |
          uv python install 3.12

      - name: Create environment
        run: |
          uv venv --python 3.12

      - name: Install Xuance with all extras
        run: |
          uv pip uninstall xuance
          uv pip install \
            -e ".[torch,tune,mujoco,atari,box2d,minigrid,metadrive,rware,einops]" \
            mpe2 \
            pytest \
            ruff

      - name: Inspect Xuance installation
        run: |
          echo "=== setup.py numpy requirement ==="
          grep -n "numpy" setup.py || true
      
          echo "=== imported xuance path ==="
          .venv/bin/python - <<'PY'
          import xuance
          from importlib.metadata import requires, distribution
      
          print("xuance.__file__ =", xuance.__file__)
          print("distribution root =", distribution("xuance").locate_file(""))
      
          print("requirements:")
          for req in requires("xuance") or []:
              if "numpy" in req.lower():
                  print(req)
          PY
      
          echo "=== installed xuance packages ==="
          uv pip list | grep -i xuance || true
            
      - name: Force NumPy version
        run: |
          uv pip install --reinstall "numpy==${{ matrix.numpy-version }}"

      - name: Check dependencies
        run: |
          uv pip check

      - name: Show environment
        run: |
          .venv/bin/python - <<'PY'
          import sys
          import numpy
          import torch
          import xuance

          print("Python :", sys.version)
          print("NumPy  :", numpy.__version__)
          print("PyTorch:", torch.__version__)
          print("Xuance :", xuance.__version__)
          print("Path   :", xuance.__file__)
          PY

      - name: Static NumPy compatibility check
        run: |
          .venv/bin/ruff check xuance \
            --select NPY201,NPY001,NPY003

      - name: Run full test suite
        run: |
          .venv/bin/pytest tests/torch -q 
